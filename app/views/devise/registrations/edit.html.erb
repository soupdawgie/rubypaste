<% provide(:title, "Update info") %>
<% provide(:button_text, "Update") %>

<div class="col-md-6 col-md-offset-3 user">
  <h1>Update info</h1>
  <%= render 'form' %>
  <%= link_to "Unhappy? Cancel your account.", registration_path(resource_name),
                                               method: :delete,
                                               data: { confirm: "Are you sure?" },
                                               class: "cancel-account" %>
</div>
