<% provide(:title, "Sign up") %>
<% provide(:button_text, "Sign up") %>

<div class="col-md-6 col-md-offset-3 user">
  <h1>Sign up</h1>
  <h4>or <%= link_to "take a tour", "/users/sign_in?guest=1" %></h4>  
  <%= render 'form' %>
</div>
